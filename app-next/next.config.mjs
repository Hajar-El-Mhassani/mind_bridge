/** @type {import('next').NextConfig} */
const nextConfig = {
    // Disable export mode to make sure we can use 'next/image' for images and utilize the Next Image Optimization
    // output: "export"
};

export default nextConfig;
